<% content_for :title do %><%= @title %><% end %>

<% content_for :extra_head do %>
    <script type="text/javascript">
        $(function() {
            $('ul.b-main-menu li.item.<%= @menu_class %> a.link')
                .addClass('selected')
                .html(function(i, html) {
                    return html + ' <b class="b-graphics b-graphics-down-arrow"><b></b></b>';
                });
        });
    </script>
<% end %>

<% content_for :left_column do %>
    <%# меню для memorues and media листингов %>
    <% if @page %>
        <%= render :partial => 'block_links', :locals => {:title => @page.title, :items => @page.pages} %>
    <% end %>
    <%= render :partial => 'projects' %>
    <%= render :partial => 'calendar' %>
    <%= render :partial => 'filters', :locals => {:show_filters => (@menu_class == 'articles'), :defaults => {:type_ids => @type_ids.join(','), :per_page => 30}} %>
<% end %>

<% content_for :content do %>
    <div class="l-column-472 l-intercolumn-28">
        <div class="b-share">
            <%= render :partial => 'social_likes' %>
        </div>
        <%= render :partial => 'articles' %>
    </div>
<% end %>

<% content_for :right_column do %>
    <%= render :partial => 'blogs' %>
    <%= render :partial => 'banners_right_column' %>
    <%= render :partial => 'social_links' %>
<% end %>

<%= render :template => 'layouts/3columns' %>