<% if params[:page] == 1 %>
    <%= render :partial => 'article_featured', :collection => @articles_featured, :as => :article, :locals => {:widgets => @widgets} %>
<% end %>
<div class="b-tiles">
    <% if @articles.count > 0 %>
        <%= render :partial => 'article', :collection => @articles, :locals => {:widgets => @widgets} %>
    <% elsif params[:query] %>
        <p>По запросу <strong><%= params[:query] %></strong> ничего не найдено.</p>
    <% else %>
        <p>С текущими условиями фильтрации материалов не найдено.</p>
    <% end %>
</div>
<% if @articles.count > 0 && params[:page] != @articles.total_pages %>
    <div class="b-more"><a class="link c-article-link" href="#">Ещё</a></div>
<% end %>